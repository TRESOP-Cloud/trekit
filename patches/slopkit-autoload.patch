diff --git a/slopkit/poops.html b/slopkit/poops.html
index df80712..55734ad 100644
--- a/slopkit/poops.html
+++ b/slopkit/poops.html
@@ -535,6 +535,7 @@ const DEFAULT_ATTEMPTS = TRIGGER === "overflow" ? 96
 const cfg = {
 
     payload: Q.get("payload") || "",
+    autoload: Q.get("autoload") || "",
     trigger: TRIGGER,
     burn: BURN,
     netprobe: Q.get("netprobe") === "1",
@@ -595,6 +596,7 @@ function exactQuery(expected) {
     const keys = Object.keys(expected);
     let count = 0, exact = true;
     Q.forEach((value, key) => {
+        if (key === "autoload") return;
         count++;
         if (!Object.prototype.hasOwnProperty.call(expected, key)
             || expected[key] !== value) exact = false;
@@ -986,7 +988,9 @@ const PAYLOADS = Object.freeze([
     { title: "PLK Manager", description: "Payload management tools.",
         name: "PLK.elf", info: "PLK.elf - PLK" },
     { title: "ShadowMountPlus", description: "Mount and manage game images.",
-        name: "shadowmountplus.elf", info: "shadowmountplus.elf - Drakmor" }
+        name: "shadowmountplus.elf", info: "shadowmountplus.elf - Drakmor" },
+    { title: "WKAL autoload", description: "", name: "payload.elf",
+        info: "payload.elf", hidden: true }
 ]);
 
 function populatePayloadButtons() {
@@ -1019,6 +1023,7 @@ function populatePayloadButtons() {
         button.appendChild(title);
         button.appendChild(description);
         button.appendChild(info);
+        if (payload.hidden) button.style.display = "none";
         button.addEventListener("click", function (event) {
             if (event && event.preventDefault) event.preventDefault();
             sendPayloadInPlace(payload.name, button);
@@ -1238,7 +1243,7 @@ function preparePayloadSender() {
     }
 }
 
-async function sendPayloadToElfldr(name) {
+async function sendPayloadToElfldr(name, base) {
     if (!elfldrSpawned)
         throw new Error("ELF loader is not ready");
     if (!P || !chain)
@@ -1250,7 +1255,7 @@ async function sendPayloadToElfldr(name) {
     if (!payloadIsListed(name))
         throw new Error("payload is not listed in this menu");
 
-    const response = await fetch("../payloads/" + encodeURIComponent(name),
+    const response = await fetch((base || "../payloads/") + encodeURIComponent(name),
         { cache: "no-store" });
     if (!response.ok)
         throw new Error("payload fetch failed: HTTP " + response.status);
@@ -2189,6 +2194,20 @@ async function runLadder() {
         flushMark("ELFLDR-MENU-INPAGE",
             "no-navigation-parked-racers-and-fds-remain-owned-port=9021");
         if (!payloadMenuEl.classList.contains("on")) showPayloadMenu();
+        if (cfg.autoload) {
+            stage("autoloading " + cfg.autoload + " in 4 s", "ok");
+            setTimeout(function () {
+                sendPayloadToElfldr(cfg.autoload, "../../payloads/").then(function (r) {
+                    flushMark("AUTOLOAD-OK", "name=" + cfg.autoload + "-bytes=" + r.bytes);
+                    stage("autoloaded " + cfg.autoload + " (" + r.bytes + " bytes)", "ok");
+                    try { window.parent.postMessage({ type: "wkal", kind: "autoload", ok: true, bytes: r.bytes }, "*"); } catch (e) { }
+                }).catch(function (err) {
+                    flushMark("AUTOLOAD-FAILED", "why=" + clean(err && err.message ? err.message : err));
+                    stage("autoload failed: " + (err && err.message ? err.message : err), "bad");
+                    try { window.parent.postMessage({ type: "wkal", kind: "autoload", ok: false, why: String(err && err.message || err) }, "*"); } catch (e) { }
+                });
+            }, 4000);
+        }
     }
 }
 
diff --git a/slopkit/poops.js b/slopkit/poops.js
index ae366f1..e8cf018 100644
--- a/slopkit/poops.js
+++ b/slopkit/poops.js
@@ -7819,9 +7819,9 @@ export function makePoopsEngine(X) {
     flushMark("STAGE5-FETCH-BIN-OK", "bytes=" + binBytes.length);
 
     try {
-      const name = o.elfName || "elfldr-ps5-1360.elf";
-      flushMark("STAGE5-ELF-FETCH-PRE", "url=../payloads/" + name);
-      const response = await fetch("../payloads/" + name, {
+      const name = o.elfName || "elfldr-ps5.elf";
+      flushMark("STAGE5-ELF-FETCH-PRE", "url=../../shared/" + name);
+      const response = await fetch("../../shared/" + name, {
         cache: "no-store",
       });
       if (!response.ok)
